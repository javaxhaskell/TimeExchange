# Supabase Local Setup

Create a `.env.local` file in the project root for local development and paste in:

```bash
NEXT_PUBLIC_SUPABASE_URL=https://lphmuftpzwbjdhnjqmrq.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_5HCm1-pTGJMKTxdRiMUFOQ_y44zx3FU
```

Do not commit `.env.local`.

The committed `.env.example` file contains placeholders only and should be used as the template for new environments.
